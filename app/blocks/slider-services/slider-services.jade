mixin slider-services()
	+b.slider-services&attributes(attributes)
		+e.content
			+e.slider
				for slide in getData('slider')
					+e.item
						+e('a').link(href="#")
							+e.image
								+e('img').icon(src="./assets/images/#{slide[0]}" alt="")
							+e.name #{slide[1]}
